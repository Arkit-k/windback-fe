# ── Backend ───────────────────────────────────────────────────────────────────
# Server-side (used in API route handlers and middleware)
BACKEND_URL=http://localhost:8080
# Client-side (used in browser-side API calls)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# ── Site metadata (SEO, sitemap, robots.txt) ──────────────────────────────────
NEXT_PUBLIC_SITE_URL=https://windback.io
NEXT_PUBLIC_APP_NAME=Windback
NEXT_PUBLIC_SUPPORT_EMAIL=support@windbackai.com

# ── Stripe Connect (optional) ─────────────────────────────────────────────────
# Set to "true" to show the one-click Stripe Connect button.
# Leave unset or "false" if Stripe Connect platform accounts are unavailable (e.g. India).
NEXT_PUBLIC_STRIPE_CONNECT_ENABLED=false

# ── Analytics (optional) ──────────────────────────────────────────────────────
NEXT_PUBLIC_GA_MEASUREMENT_ID=
